from code import fib
import pytest

def test_(fib):
    # Arrange
    # Act
    # Assert
    between = 1
    between_2 = 0
    assert fib(between) == 1
    assert fib(between_2) == 0
    list = [1, 0]
    less_than = n
    if less_than not in list:
        assert fib(less_than) == (less_than-1) + (less_than-2)
    if fib(n) >= 2:
        assert (n-1) + (n-2) == (n-1) + (n-2)
